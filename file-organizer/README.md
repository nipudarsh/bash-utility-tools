# File Organizer (Bash Script)

This tool automatically organizes files in a directory into folders based on file type.

## Features
- Automatically creates folders
- Supports videos, images, audio, documents, archives, scripts, and more
- Safe (does not affect directories)
- Works on any folder

## Usage

1. Copy the script into the target directory
2. Make it executable:
   ```bash
   chmod +x organize.sh

